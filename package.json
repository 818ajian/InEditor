{
  "name": "ineditor-refactoring",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "bson": "^2.0.5",
    "earcut": "^2.1.3",
    "express": "^4.16.2",
    "fs": "0.0.1-security",
    "socket.io": "^2.1.0",
    "xml-beautifier": "^0.3.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
